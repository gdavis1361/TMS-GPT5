<?php 
/**
 * @author Reid Workman
 */
 
class _PrintTable {
	function __construct( $aTableInfo, $sFocusColumn = FALSE, $sOrder = 'DESC' ) {
		if ( is_array($aTableInfo) ) {
			if( count($aTableInfo) > 0 ) {
				//foreach ( $aTableInfo as $sTitle => $)
			}	
		}
		//throwError( '' );
		return false;
	}
}
?>